CREATE TABLE users(
id INT NOT NULL PRIMARY KEY AUTO_INCREMENT,
tel VARCHAR(11) PRIMARY KEY,
pw VARCHAR(20) PRIMARY KEY
);

CREATE TABLE tag_order(

ordername VARCHAR(30),
tag VARCHAR(20) NOT NULL
);

CREATE TABLE tag_list(
tag VARCHAR(20) NOT NULL PRIMARY KEY,
counts INT(5) NOT NULL
);